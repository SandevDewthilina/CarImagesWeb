@model List<Tag>

@{
    ViewBag.Title = "All Tags";
}

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-12 d-flex justify-content-between">
                    <h1>All Tags</h1>
                    <a asp-action="CreateTag" class="btn btn-primary" style="margin-left: 10px">Create Tag</a>
                </div>
            </div>
        </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">

        <div class="container-fluid">
            <div class="row">
                @if (Model.Any())
                {
                    foreach (var tag in Model)
                    {
                        <div class="col-3">
                            <div class="card  mb-3">
                                <div class="card-header">
                                    Tag Id: @tag.Id
                                </div>
                                <div class="card-body">
                                    <h5 class="card-title">@tag.Name</h5>
                                    <h5 class="card-text">@tag.Code</h5>
                                </div>
                                <div class="card-footer">
                                    <a asp-route-id="@tag.Id" class="btn btn-warning">Edit</a>
                                    <a href="#" class="btn btn-danger">Delete</a>
                                </div>
                            </div>
                        </div>
                    }
                }
                else
                {
                    <div class="card">
                        <div class="card-header">
                            No tags created yet
                        </div>
                        <div class="row">
                            <div class="col-8">
                                <a class="btn btn-primary" style="margin: 25px" asp-action="CreateTag">Create Tag</a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>


    </section>
    <!-- /.content -->
</div>
<!-- /.content-wrapper -->